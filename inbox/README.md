# Inbox

Drop newspaper PDFs here, commit, then run the workflow.

Files are tracked by a hash of their **contents**, not their filename, so a
paper is never read twice even if you re-upload it under a different name.
Delete PDFs from this folder whenever you like — already-read papers will not
be re-read if you put them back.
